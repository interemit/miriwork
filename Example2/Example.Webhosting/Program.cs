namespace Example.Webhosting
{
    public class Program
    {
        public static void Main(string[] args)
        {
            // Main-method is needed, if Microsoft.Net.Sdk.Web is used in csproj
            // -> otherwise you will get a build error
            // -> but this method will not be called
        }
    }
}
